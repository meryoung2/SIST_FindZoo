<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="deal">
	<select id="findAll" resultType="dealVo">
		SELECT board_num, title, content, bdate, views, board_type_num, member_num, member_nick
		FROM (SELECT rownum n, board_num, title, content, bdate, views, board_type_num, member_num, member_nick
		FROM (SELECT board_num, title, content, bdate, views, board_type_num, MEMBER.member_num, member_nick 
		FROM board, member WHERE BOARD_TYPE_NUM = 33
		AND board.member_num = MEMBER.member_num
		ORDER BY board_num DESC))
		WHERE n BETWEEN #{start} AND #{end}
	</select>
	
	<insert id="insertBoard" parameterType="dealVo">
		insert into board(board_num,title,content,bdate,views,board_type_num,member_num) values((select nvl(max(board_num), 0)+1 from board), #{title}, #{content}, sysdate, 0, 33, 1)
	</insert>
	
	<insert id="insertDeal" parameterType="dealVo">
		insert into deal(deal_num,deal_price,board_num) values((select nvl(max(deal_num), 0)+1 from deal),#{deal_price},(select nvl(max(board_num), 0) from board))
	</insert>
	
	<insert id="insertDealPicture" parameterType="dealVo">
		insert into picture(picture_file_num, picture_fname, board_num) values((select nvl(max(picture_file_num), 0)+1 from picture),#{picture_fname},(select nvl(max(board_num), 0) from board))
	</insert>
	
	
	<select id="getBoard" resultType="dealVo">
  		select * from deal d, board b, picture p, member m where d.board_num = b.board_num and p.board_num = b.board_num and m.member_num = b.member_num and b.board_num=${board_num}
  	</select>
  
	<select id="totalRecord" resultType="int">
  		select count(*) from deal
	</select>
	  
	<update id="updateHit" parameterType="int">
	  	update board set views = views+1 where board_num = #{board_num} and board_type_num = 33
	</update>

	<update id="updateDeal" parameterType="dealVo">
	  	update deal set deal_price = #{deal_price} where board_num = #{board_num}
	</update>
	
	<update id="updateBoard" parameterType="dealVo">
  		update board set title=#{title},content=#{content} where board_num = #{board_num}
  	</update>
  	
  	<update id="updateDealPicture" parameterType="dealVo">
  		update picture set picture_fname = #{picture_fname} where board_num = #{board_num}
  	</update>
  	
  	<delete id="deleteDeal">
  		delete deal where board_num = #{board_num}
  	</delete>
  	
  	<delete id="deleteBoard">
  		delete board where board_num = #{board_num}
  	</delete>
  	
  	<delete id="deleteDealPicture">
  		delete picture where board_num = #{board_num}
  	</delete>

</mapper>