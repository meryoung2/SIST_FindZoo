<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
	<select id="mainFind" resultType="findVo">
		<![CDATA[select board_type_num, title, content, bdate, find_lost_loc, picture_file_num, picture_fname from (select * from find f, board b, picture p, member m where f.board_num = b.board_num and p.board_num = b.board_num and m.member_num = b.member_num order by bdate desc) where rownum <=3]]>
	</select>
	
	<select id="mainBoho">
		<![CDATA[select board_type_num, title, content, bdate, boho_find_loc, picture_file_num, picture_fname from (select * from boho bh, board b, picture p, member m where bh.board_num = b.board_num and p.board_num = b.board_num and m.member_num = b.member_num order by bdate desc) where rownum <=3]]>
	</select>
</mapper>